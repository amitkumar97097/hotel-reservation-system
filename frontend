// === FRONTEND with React Router ===
// Add this to App.jsx or main entry point

import React from 'react';
import { BrowserRouter as Router, Routes, Route, Link } from 'react-router-dom';
import HotelRoomReservationSystem from './HotelRoomReservationSystem';

const About = () => (
  <div className="p-4">
    <h1 className="text-xl font-bold mb-4">About This Project</h1>
    <p>This app is a hotel room reservation system that prioritizes minimal travel time and optimal room layout.</p>
  </div>
);

const App = () => (
  <Router>
    <nav className="flex gap-4 p-4 bg-gray-100 border-b mb-4">
      <Link to="/" className="text-blue-600">Home</Link>
      <Link to="/about" className="text-blue-600">About</Link>
    </nav>
    <Routes>
      <Route path="/" element={<HotelRoomReservationSystem />} />
      <Route path="/about" element={<About />} />
    </Routes>
  </Router>
);

export default App;

// Don't forget to install react-router-dom in frontend:
// npm install react-router-dom

// Then in index.js or main.jsx:
// import React from 'react';
// import ReactDOM from 'react-dom/client';
// import App from './App';
// ReactDOM.createRoot(document.getElementById('root')).render(<App />);
